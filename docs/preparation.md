# 开发前准备

日期：2026-09-07。P0 已落实协议、合成校验、运行时实验和接入清单；下面区分准备工作与尚待实现的产品行为。

## 开工前需要完成的准备

| 准备项 | 当前状态 | 交付物 / 完成标准 |
| --- | --- | --- |
| 产品范围 | 已确定方向 | CLI、三个首批项目、动作/结果/知识闭环；不扩展生产自动修复 |
| 技术栈 | 已选 Python 3.12/Linux | Python/Node 合成子进程实验均通过；按维护成本选 Python，见 runtime-decision |
| 三份数据契约 | Schema 0.1.0 与校验已完成 | JSON Schema、合法/非法样例、版本策略、状态映射 |
| 项目注册 | ID/路径规则已确定；加载器待 P1 | 三个项目的当前路径、稳定 ID、checkout 去重和索引白名单 |
| 动作清单 | 三份声明已校验；原生执行待 P1 | 精确命令/参数、副作用、前置依赖、资源锁、解析器与必需产物 |
| 验收样本 | 合成协议样例已完成；行为测试待 P1 | 脱敏 fixture 覆盖成功、失败、零测试、跳过、超时、取消及报告缺失 |
| 知识样本 | 白名单和样本文档已选；索引待 P2 | 三项目少量已验证事故/测试文档，明确来源、答案与适用范围 |
| 存储与恢复 | 规则已确定；恢复行为待 P1/P2 | 可重建索引，证据导出恢复，删除传播和引用完整性 |
| 执行隔离 | 参数/路径校验与取消探针通过；Runner 待 P1 | 参数校验、资源锁、进程组取消、凭据保护及日志脱敏 |
| 模型接入 | 首阶段可不依赖 | 首先完成无模型可复现闭环；后续明确数据外发、预算和失败处理 |
| 发行策略 | 未选择 | 公开发行前确定名称、许可证和依赖许可；不阻塞本地准备 |

## 技术栈选择应验证什么

本机 Python 3.12.3 和 Node.js v20.19.2 已完成 [运行时探针](runtime-decision.md)。仅在本项目 `.venv` 安装并固定了 JSON Schema 校验依赖，没有升级系统运行时或安装源项目依赖。Agent Mail 当前机器 Runner 是 Python；此次没有调用它，原生接口对照在 P1 进行。

不要以某个模型/API/框架的宣传或旧环境记录确定技术栈。运行时支持版本和依赖锁定在开工时核查；本轮不要求安装 LangChain、向量数据库或 Web 框架。

## 样本和验收集

首批动作逐个复核：Agent Mail 的类型化 SOP 动作、AI Asset Hub 的本地检查、homelab-doctor 的 fixture 检查。复核真实命令并不等于获准立即运行全部测试，尤其安装器、网络和共享数据库动作。

先使用合成 fixture 证明协议。必须覆盖：

1. 正常成功和断言失败，保留原生退出码及报告。
2. 退出 0 但没有测试、全部跳过或缺少报告，不误报 PASS。
3. 不支持环境和缺少依赖，区别于产品缺陷。
4. 超时、取消、子进程遗留、协调器中断和再次启动。
5. 同仓库不同 worktree、同共享资源的锁冲突。
6. 日志中的密钥样本及指令注入文本；不泄露、不执行。
7. 文档新增、修改、移动、删除；不检索到旧版本的当前结论。
8. 两个项目同名文档和相似事故；不串用项目私有知识。

选择历史事故做诊断评测时，隔离修复后提交和答案文档，避免把答案泄漏当成诊断能力。记录原人工流程耗时、建议有效性、人工修改量和模型用量；不预设效率提升百分比。

## P0 退出条件

- 三个项目各至少一个安全、边界明确的目标动作完成登记。
- 三类契约有可验证的样例与失败样例，主代理审查通过。
- 技术栈和运行环境有一份实验结论，不再保留互相矛盾的默认选择。
- 证据目录、索引范围、锁键、模型数据边界和验收集已明确。
- 每个实现任务拥有指定文件范围和可执行验收标准。

以上准备产物见 [P0 验收边界](p0-acceptance.md)、[试点动作](pilot-actions.md) 和 [三类契约](contracts.md)。P0 本地检查通过后，下一阶段开始 P1 fixture Runner 与单项目适配器实现。无须为了架构草案完整而等待 Web、远程调度、生产日志或发行决策。

## 后续需要单独确定的外部事项

远端仓库位置/可见性、公开名称和许可证、在线模型凭据与数据外发策略、真实环境执行范围。当前没有创建远端、上传内容、联系文章作者或写入任何外部记忆服务。实际涉及这些事项时再按已获授权推进；不把它们变成本地开发的额外前置阻碍。

用户已明确：本项目后续 push 统一由用户手动执行。代理只准备本地提交，不执行 push 或等效远端上传。
